[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "*"

[dev-packages]

[requires]
python_version = "3.8"

[scripts]
dev="flask run --host=0.0.0.0 --port=5000"
prod="flask run --host=0.0.0.0 --port=5000"
